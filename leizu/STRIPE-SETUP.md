# Stripe setup moved

Use `leizu/LEIZU-PIPELINE-SETUP.md` for the current Leizu configuration. It covers the Stripe redirect, Payment Link IDs, Netlify variables, Cal.com webhook, Resend domain, and the full live test sequence.
