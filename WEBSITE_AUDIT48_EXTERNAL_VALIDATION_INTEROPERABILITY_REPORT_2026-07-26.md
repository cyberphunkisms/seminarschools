# Website Audit 48 — External Validation, Interoperability, and Final Efficiency Pass

Date: July 26, 2026  
Release: `2026-07-26-site-audit48-external-validation-interoperability-final`  
Status: PASS for all locally executable and authorized work; native, vendor-account, and human-observation rows remain explicitly external

## Outcome

Audit 48 completed the executable portion of the external-validation work carried forward from Audit 47 and performed another focused technical-efficiency pass over calendar production, assistive-technology prerequisites, cross-engine readiness, live-source harvesting, release orchestration, and evidence portability.

The release now has:

- strict, independently parsed calendar output across all 857 ICS files;
- repository-wide assistive-technology prerequisite checks across 3,389 source HTML documents;
- a permanent 24-scenario Firefox/WebKit execution program with an honest blocked-runtime preflight;
- bounded, read-only verification of 200 configured live-source bindings and 219 unique current endpoints;
- production-budget seminar and protest dry-runs with passing source-health gates;
- deterministic Audit 48 release stamping, ordered report dependencies, and package/release blockers;
- exact once-weekly scheduling preserved for every content and link workflow.

Native branded browsers, native screen readers, physical devices, real-user observation, calendar-vendor account imports, and post-deployment subscription refresh cannot be truthfully completed in this Linux workspace. They are specified by exact protocols and remain unclaimed.

English remains the translation source of truth. Organizer-authored text remains verbatim and visibly source-language bounded. High-stakes Leizu translations remain noindex pending bilingual review. BB remains teacher-led and has no site-owned session runner. Frozen historical audit evidence remains untouched.

No security audit was performed.

## Implemented findings

| Priority | Finding | Implemented result | Permanent evidence |
|---|---|---|---|
| P1 | Ten focused calendar feeds contained 1,416 continuation lines of 76 octets because the mandatory folding SPACE was not included in the line budget. | Continuation content now uses a 74-octet budget, so every physical line in all 857 files is at most 75 octets. UTF-8 code points remain intact. | Nine-test calendar-client suite and independent full-corpus parse. |
| P1 | `deadlines.ics` used LF-only transport framing and was no longer byte-identical to `opportunities.ics`. A strict reader could interpret its 84,959 bytes as one line. | The historical alias now preserves generated bytes exactly, uses CRLF throughout, and is byte-identical to the canonical feed. | Full-corpus transport and legacy-identity checks. |
| P1 | Organizer text escaping did not normalize an isolated carriage return. | The builder now escapes CRLF, LF, and bare CR consistently; pathological punctuation, Unicode, newline, daylight-saving, all-day, multiday, and legacy-ID fixtures are permanent. | Calendar edge-case and round-trip tests. |
| P1 | Calendar delivery depended on extension inference despite a `nosniff` response policy. | Deployment headers now declare `text/calendar; charset=utf-8` for event and feed trees. English, French, static, and dynamic calendar links declare `type="text/calendar"`. | Local HTTP byte/MIME tests and route checks. |
| P1 | Existing calendar checks did not establish independent parser acceptance or complete client-facing semantics. | Pinned `icalendar 7.2.0` parses and round-trips 4,172 VEVENT representations while preserving stable UID, time, all-day, duration, source, identity, and focused-feed semantics. | `scripts/reports/audit48-calendar-client-interoperability.json`. |
| P1 | Native screen-reader work could not be inferred from generic HTML checks, and it lacked a release-specific sign-off matrix. | Added a 3,389-document prerequisite gate plus exact macOS VoiceOver, Windows NVDA, iPhone VoiceOver, Android TalkBack, and real-user protocols. | `scripts/reports/audit48-assistive-technology.json` and `AUDIT48_NATIVE_DEVICE_AT_TEST_PROTOCOL_2026-07-26.md`. |
| P1 | Firefox/WebKit validation lacked a portable, release-bound program, but running a patched engine must not be relabelled as branded Firefox or Safari. | Added 12 route/viewport scenarios for each of Firefox and WebKit, 24 intended engine scenarios total. The preflight records zero runtime checks and the actual local security-policy blocker. | `data/audit48-browser/cross-engine-preflight.json` and a 23-assertion program verifier. |
| P1 | Audit 47 had no current endpoint evidence for the scheduled source inventory. | Performed 219 unique bounded GETs across 227 endpoint bindings, 200 source bindings, and 185 hosts. The report retains exact HTTP, block, challenge, timeout, and client-rendered classifications without claiming unreachable sources passed. | `scripts/reports/audit48-live-harvest-endpoints.json`. |
| P1 | Source availability alone did not prove the scheduled selections could still meet their health floors. | The next seminar selection processed all 96 selected sources and found 133 events; 29 authoritative primary sources exceeded the floor of 24, and 12 critical sources exceeded the floor of 5. The protest selection processed all 16 primaries plus two corroboration rows and found 22 unconfirmed events; 9 authoritative primaries exceeded the floor of 4, and 2 critical sources exceeded the floor of 1. | Release-bound scheduled dry-run evidence and permanent unit/verifier gates. |
| P1 | Browser/OCR failures could be flattened into generic failures or trigger repeated unbounded retries. | Navigation is bounded, commit-mode failure kinds are preserved, and all ten selected protest browser sources were attempted. The evidence honestly records ten navigation timeouts, one browser-network timeout, and zero rendered documents in this environment. | Live-harvest evidence composition, verifier, and focused tests. |
| P2 | Live probing could waste time or load a host too aggressively. | Requests are deduplicated to unique URLs, capped at eight workers and two concurrent requests per host, use bounded connect/read timeouts, and sample at most 262,144 bytes per endpoint. | Live-harvest report limits and verifier. |
| P2 | Audit 48 report generation could race its prerequisite evidence or be rewritten needlessly. | Calendar, assistive, browser-preflight, live, and aggregate evidence now run in dependency order. Report writers compare rendered bytes before replacement and accept deterministic output timestamps. | Full-runner, build-efficiency, release-gate, and predeploy contracts. |
| P1 | Regenerated HTML could retain the Audit 47 asset token after substantive Audit 48 JavaScript and delivery changes. Applying that migration after localized routes also made twelve English-source hashes stale on a first build from Audit 47. | The canonical build now applies the Audit 48 release stamp after English route generation and before localized routes bind their source hashes, then builds the manifest and public tree. Source/public and translation gates reject stale hashes or mixed asset ownership. | Audit 48 release-stamp, build-order, translation, release, and parity checks. |
| P1 | The new release stamper and the active frozen Audit 43 UI applicator could traverse a transient `.rsync-tmp` directory and then read an enumerated file after it disappeared. | The stamper now uses the shared generated-directory policy and tolerates a disappearing file. A current Audit 48 wrapper applies that policy to the frozen Audit 43 applicator without changing the SHA-locked historical program. | Repository-walk policy now covers 27 scanners and both packagers; frozen Audit 43 verification remains unchanged. |
| P1 | The packagers' top-level work-directory pattern also matched the new `AUDIT48_…` protocol filenames, so their own required-member check rejected both protocols. | Work-tree exclusion now applies only when the matching top-level component contains a descendant file. Top-level Audit-named deliverables remain selectable while disposable extracted Audit directories remain excluded. | Both packagers require and include the native-AT and calendar-vendor protocols; the repository-walk gate enforces directory-only scoping. |

## Calendar interoperability result

- Tests: 9 of 9 passed.
- Canonical event records and files: 833.
- Explicit legacy event aliases: 12.
- Single-event ICS files: 845.
- Focused and historical feed files: 12.
- Total ICS files: 857.
- Independently parsed and round-tripped VEVENT representations: 4,172.
- Maximum physical line: 75 octets.
- Transport violations, parser failures, identity mismatches, and source-semantic mismatches: zero.

The local evidence proves the RFC 5545 file, identity, time, parser, link, delivery-header, and deterministic edge-case contracts. RFC 5545 defines the iCalendar content-line and component format: https://www.rfc-editor.org/rfc/rfc5545

Actual vendor-account behavior remains external. Google documents desktop ICS import at https://support.google.com/calendar/answer/37118?hl=en, Apple documents `.ics` import at https://support.apple.com/guide/calendar/import-or-export-calendars-icl1023/mac, and Microsoft documents Outlook import and subscription at https://support.microsoft.com/en-us/office/import-or-subscribe-to-a-calendar-in-outlook-com-or-outlook-on-the-web-cff1429c-5af6-41ec-a5b4-74f2c278e98c

The exact four-file and subscription procedure is in `AUDIT48_CALENDAR_VENDOR_IMPORT_PROTOCOL_2026-07-26.md`. It requires temporary vendor calendars, exact-time and all-day verification, legacy/canonical UID identity, post-deployment subscription, and a second refresh after one real event update.

## Assistive-technology result

The static prerequisite gate passed:

- source HTML documents: 3,389;
- interactive documents: 2,517;
- redirect documents: 872;
- static IDs: 10,378;
- static ARIA ID references: 2,726;
- skip links: 2,513;
- images checked: 38;
- buttons checked: 1,136.

The release blocks missing document languages, incorrect main or H1 structure, positive `tabindex`, duplicate IDs, unresolved static ARIA references, unresolved skip links, missing image alternatives, unnamed buttons, and regressions in the existing keyboard/input/accessibility gates.

This is prerequisite evidence, not a simulated native screen-reader pass. Apple’s VoiceOver guide is at https://support.apple.com/guide/voiceover/welcome-voic010/mac and its landmark-navigation guidance is at https://support.apple.com/guide/voiceover/vo35709/mac. The NVDA user guide is at https://download.nvaccess.org/documentation/en/userGuide.html and official help/training is at https://www.nvaccess.org/get-help/

The required native and human-observation rows are defined in `AUDIT48_NATIVE_DEVICE_AT_TEST_PROTOCOL_2026-07-26.md` and the reusable sign-off issue template.

## Cross-engine result

The permanent program covers 12 representative route/viewport scenarios in each of Playwright Firefox and Playwright WebKit, for 24 intended engine scenarios. It includes localized LTR and RTL routes, a high-stakes noindex intake route, Polymythcal browser/detail/submission routes, Teacher Resources filtering, BB Chinese content, Methodology search, Campaigncodex search, and Saul Persian mobile layout.

Runtime checks executed in this environment: 0.

The local browser run was blocked by the runtime’s local-site browser security policy, and the WebKit binary was unavailable. The preflight records that blocker instead of manufacturing a pass. Playwright’s own browser documentation explains that its Firefox is patched and that its WebKit build is not branded Safari: https://playwright.dev/docs/browsers

Accordingly, branded Firefox and native macOS Safari remain external even after a future supplemental Playwright run passes.

## Current live-source result

The read-only endpoint probe covered:

- 96 seminar sources: 48 HTTP-usable, 5 browser-required, and 43 unreachable or unsupported;
- 18 protest source bindings: 7 HTTP-usable, 2 browser-required, and 9 unreachable or unsupported;
- 86 festival sources: 39 HTTP-usable, 5 browser-required, and 42 unreachable or unsupported.

Across 219 unique requests, 97 endpoints were reachable, 34 were blocked, 35 returned HTTP errors, 51 timed out, and 2 were client-rendered shells. These classifications are evidence about current upstream behavior, not pass/fail relabelling.

The seminar and protest production-budget source-health gates passed. Festival validation covered all 86 configured discovery and official-primary endpoints, but the scheduled extractor’s paid-agent stage was not invoked. The protest browser/OCR stage was attempted under bounded navigation and was environment-blocked. No publication, external mutation, source-data write, paid-agent invocation, or cadence change occurred.

Exactly once-weekly schedules remain:

- seminars: `47 8 * * 1`;
- festivals: `42 9 * * 2`;
- protests: `18 8 * * 3`;
- external links: `17 10 * * 0`.

## Validation and release blockers

The Audit 48 aggregate gate requires and binds:

- the current release and asset identifiers;
- all calendar, assistive, browser-preflight, and live-source evidence;
- the exact source inventories and material metrics;
- explicit external-execution boundaries;
- exact once-weekly workflow cadence;
- permanent package commands;
- both native/vendor protocols;
- the no-security-audit boundary.

The canonical build, source/public parity, full fresh runner, full reuse runner, deterministic archive creation, embedded archive integrity, and final SHA-256 checksum are shipping blockers. Their final counts are recorded with the completed package rather than predicted in this report.

## External validation still required

The following work needs environments, accounts, devices, or people not available here:

1. Branded Firefox execution.
2. Native macOS Safari execution.
3. macOS Safari with VoiceOver.
4. Windows Firefox and Chrome with NVDA.
5. Physical iPhone Safari with VoiceOver.
6. Physical Android Chrome with TalkBack.
7. A consented real-user task-completion and comprehension session.
8. Google Calendar, Apple Calendar, and Outlook imports using the exact release fixtures.
9. Post-deployment subscription refresh in all three vendors, followed by a stable-UID update check.
10. A successful protest browser/OCR capture in an environment that can reach the selected sources.
11. The next authorized festival paid-agent extraction, without increasing its once-weekly cadence.

None of these rows is labelled passed by static analysis, a patched browser engine, a file parser, or an endpoint probe.

## Permanent commands

```text
npm run verify:audit48-assistive-technology
npm run verify:audit48-browser-program
npm run test:audit48-calendar-clients
npm run verify:audit48-calendar-clients
npm run test:audit48-live-harvest
npm run verify:audit48-live-harvest
npm run verify:audit48-live-harvest:current
npm run verify:audit48-external-validation
```

`npm run audit:audit48-cross-engine-browser` is an explicit environment-dependent execution command. It is intentionally excluded from the portable full runner and must not replace native branded-browser sign-off.
