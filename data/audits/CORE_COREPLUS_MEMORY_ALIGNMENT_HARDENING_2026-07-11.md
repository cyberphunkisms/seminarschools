# CORE, CORE+, and memory alignment hardening

Date: 2026-07-11

## User rulings applied

1. Food, nutrition, and health context is not treated as disposable merely because it sits outside behavioral CORE. It can support the website nutrition section and future relevant work.
2. Duplicated behavioral rules indicated weak enforcement. The active copies were consolidated into one canonical slot and hardened with trigger-specific pre-response checks.
3. Historical slot generations no longer compete inside active CORE+. Exact prior records remain preserved in a dedicated CORE history archive.
4. The old Methodologylist section named `core` was a framework-content section rather than the assistant behavioral CORE. It is now labeled and routed as `Framework Core` to remove the naming collision.
5. Polymyth work now has a scoped bootstrap rule. The AI searches the current working files first, loads the current full website ZIP and CORE+, and asks for the current full ZIP only when those materials are genuinely absent.
6. Persistent memory was explicitly cleaned by user instruction. Duplicate behavioral summaries, superseded release/deployer state, temporary travel state, and expired job-application state were routed out of memory. Stable personal context and food, nutrition, and health context were retained. The remaining project trigger is the compact CORE bootstrap plus the explicit-only memory lock.

## Active CORE+ consolidation

- Added `CORE CURRENT MAP` as the sole current slot authority.
- Added current hardened slot 24 `POLYMYTH CORE BOOTSTRAP`.
- Merged the six anti-yap enforcement triggers into current slot 22.
- Merged `AUDIT DEPLOYMENT FIRST` and `LEGIBILITY OVER POETRY` into current slot 28 correction defaults.
- Merged the full explicit-only memory discipline into current slot 23 R38.
- Renamed the live Ouroborosanalyses record as an active dispatch handler rather than a second slot 2 mirror.
- Moved displaced, retired, consolidated, and superseded slot records to `CORE HISTORY`.

## Space and clarity changes

- Active CORE+ entries: 46.
- Historical CORE records: 18.
- Exact historical bodies are preserved at:
  - `polymyth/archive/core-history/core-history-full-records-2026-07-11.md`
  - `polymyth/archive/core-history/core-history-full-records-2026-07-11.json`
- Searchable CORE history entries now use compact stubs, preventing old full rule bodies from filling active runtime context.
- Methodologylist canonical HTML decreased by roughly 73 KB after historical compaction while retaining the exact records in the archive.

## Bootstrap rule

For polymorphousmythology, Meaninglib, Seminar Schools, website, book, or star-file work:

1. Search current attachments and the working directory for the full website ZIP or extracted site tree.
2. Load `CHARTER.txt`, Methodologylist, CORE+, and relevant sibling star files.
3. Use the CORE CURRENT MAP. Historical records never override current slots.
4. Ask for the current full website ZIP only when the substrate is genuinely absent.
5. Treat remembered summaries as pointers rather than current project state.

## Memory alignment

Persistent memory now carries the smallest required runtime instructions:

- `CORE MEMORY LOCK`: no memory write, edit, consolidation, or deletion without explicit user instruction.
- `POLYMYTH CORE BOOTSTRAP`: locate and load current ZIP and CORE+ before polymyth work, or ask for the current full ZIP when absent.

Expanded operational language remains in CORE+. Project history, source corpora, release state, and worked examples remain in files.

## Route clarification

The former `/polymyth/methodologylist/core/` route held two framework-topic entries and was unrelated to behavioral CORE. Those entries now live at:

`/polymyth/methodologylist/framework-core/`

The old route redirects permanently to the clarified route.

## Verification

The rebuilt full website passed all 81 release checks.
